import 'package:flutter/material.dart';

class NUSCardScreen extends StatelessWidget {
  const NUSCardScreen({Key? key}): super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
          child: Text('NUS Card Route'),
      ),
    );
  }
}
